<view class="welcome">
 <view class="table_box">

<view>
       <block  wx:for="{{list}}" wx:key="key">
            <span index="{{index}}" data-id="{{item.id}}">{{item.clientName}}</span>
           
      </block>
</view>




 </view>
</view>
